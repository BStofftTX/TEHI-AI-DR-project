export function runModel() {
  throw new Error(
    'TensorFlow Lite adapter not implemented yet. Add .tflite model artifacts and preprocessing configuration before enabling production inference.'
  );
}
